"use client";

/*
 * Customer widget — symmetry with /widget/engineer.
 *
 * The Relay desktop shell shows an always-on-top glowing orb on the desktop.
 * In customer mode, clicking the orb opens the main window to /room where
 * the existing RoomClient handles the full customer call lifecycle
 * (start-guest-call, waiting state, Zoom embed when claimed).
 *
 * This route is a placeholder for a future hidden customer service window —
 * e.g. for "engineer is joining" or "call timeout" desktop notifications
 * that work even when the main window is closed.
 */

export function CustomerWidgetClient() {
  return null;
}
