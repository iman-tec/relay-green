"use client";

/*
 * TODO(integration): placeholder stub.
 *
 * `feat/engineer-dashboard-redesign-2026-05-27` references this component
 * (imported + rendered in EngineerSessionClient.tsx) but the real source
 * file was never committed to the branch. This stub keeps the integration
 * branch compilable while the real "pull a supervisor into the session"
 * button is shipped.
 *
 * Remove this file once the real component lands.
 */
export function EngineerEscalateButton(_props: { sessionId: string }) {
  return null;
}
